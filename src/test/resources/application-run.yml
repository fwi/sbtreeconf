# sbtreeconf run configuration

management.server.port: 8081

# Note that devtools sets server.error.include* properties to show all 
# and additional warnings are logged with ExceptionHandlerExceptionResolver.
# To prevent that, remove devtools as dependency from the pom.xml
# (turning this setting off has no effect).
spring.devtools.restart.enabled: true
